{
  "name": "shiftproof",
  "version": "0.1.0",
  "private": true,
  "description": "Lightweight web app for restaurants, cafés, and boutique hotels to complete recurring shift routines, capture proof, and track exceptions.",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace apps/web",
    "build": "npm run build --workspace apps/web",
    "start": "npm run start --workspace apps/web",
    "lint": "npm run lint --workspace apps/web",
    "typecheck": "npm run typecheck --workspace apps/web",
    "format": "echo \"[shiftproof] No formatter configured yet.\" && exit 0",
    "check": "npm run typecheck --workspace apps/web && npm run lint --workspace apps/web"
  }
}
